/**
 * ランキング表示の設定
 */
export const RANKING_CONFIG = {
  TOP_LIMIT: 10,         // ランキング画面に表示する上位の人数
  /**
   * 参加者がこの人数に達するまでは「集計期間」とし、順位を公開しない。
   * 参加者が少ないうちに「1位 / 1人」のような過疎に見える表示を出さないため（難易度ごとに判定）
   */
  MIN_PARTICIPANTS: 10,
} as const;
